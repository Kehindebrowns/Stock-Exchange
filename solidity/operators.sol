// SPDX-Lisence-Identifier:MIT

pragma solidity ^0.8.0
contract Myoperators {
     uint8 = a;
     uint8 = b;
     uint8 = c;
      bool myBool =false;  
     a = a * b + c;
     a % b;
     a--
     b ++
     b*= c ;
     bool results = false;
     if (a <= b)
      return true;
     if (a !=b)
     return true;
       if (a <= b &&c > d){
       return true;
       }
       if (a <= b){
        return true;
       }
       if (a < b && a>20)
       return false;
       return =!return
       if (bool) = true;
       uint8 bool = a < b ? 100:50;
       if (a < b || b < c;) 
      uint8 return;
      return = a < b && c > b;
}