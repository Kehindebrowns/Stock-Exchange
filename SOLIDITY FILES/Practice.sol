// SPDX-Lisence-Identifier:MIT
pragma solidity ^0.8.0;

contract Practice{
    // Iternable mapping
    mapping(address=> bool)public balances;
    mapping (address => uint) public inserted;
    address[]public keys;
    function getAddress(address_key uint_val) external returns(address){
          balance._key =value;
    }
    }
    function getValue() external view returns (uint) {
        return values;
        values.length[_value];
    }
    // Structs
    // SPDX-Lisence-Identifier:MIT
    pragma solidity ^0.8.0;
    contract Practice{
        struct Cars {
        string model;
        uint year ;
         address owner; 
         }
         Car public myCar;
         constructor(){
             Car (model,year,owner); ((Toyota,1990,msg.sender));
          }
        
         
    }
      
         
        // the cars.push function is used for moving certain amounut of items
        // Array Shift..
        uint []public array;
        arr[1,2,3];
                function remove(uint_index) extenal view{
            require(_index<array.length,index"out of bound");
            for (uint i = index; i < array.length; i++) {
                arr[i]=arr[i+1];
            }
        }    
        }
         contract Event{
            event Log(string message,uint val);
            function example() extenal returns (message,val) {
                emit.log("Kehinde",54);
            }
            event Message(Indexed Address_for,Indexed Address_to,string message);
            function example() extenal{
                emit Message(_for,msg.sender);
            }
            // using immutable function...
            address public immutable owner;
            uint public x;
            function foo() public returns{
                    x=+1;
            }
            constructor ( addressname) public {
                require(msg.sender==owner);
            }
         }
      }


