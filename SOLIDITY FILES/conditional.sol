// SPDX-Lisence-Identifier:MIT
pragma solidity ^0.8.0;

contract Myconditional{
    uint8 public number= [1,2,3,4,5,6,7,8];
      function isEvenNumber (uint_number) public view return(bool) {
        if value (_number _2);
        return  = true;
            else{
                return = false;
            }
                     
      }

       contract Myloops{
            function countEvenNumber () public view return (uint){
                unit count = 0;
                for (unit) 0; i < Number .lenght ([i]) }
                
                 
            }

       }

}